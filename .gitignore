# User configuration (contains personal URLs and paths)
config.yaml

# Local Claude overrides
CLAUDE.local.md

# OS files
.DS_Store

# Personal data directories (contain CV content, analysis, outputs)
sources/
outputs/
analysis/

# Personal agent context (built by Claude during setup)
AGENTS.md

# Node dependencies (can be reinstalled)
node_modules/
